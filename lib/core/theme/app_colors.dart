import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors
  static const Color primaryDark = Color(0xFF004D4E);
  static const Color primaryMedium = Color(0xFF00595A);
  static const Color seedColor = Color(0xFFF6F1EB);

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.black87;

  // Background Colors
  static const Color backgroundDark = Color(0xFF00595A);
  static const Color backgroundLight = Color(0xFFF6F1EB);

  // Semantic Colors
  static const Color error = Colors.red;
  static const Color success = Colors.green;
  static const Color warning = Colors.orange;
}
